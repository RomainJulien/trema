<div class="dashboard-container">
  <!-- 🔹 En-tête -->
  <h1 class="dashboard-title">Tableau de bord</h1>

  <!-- 🔹 Section principale -->
  <div class="dashboard-main">
    <!-- 🔹 Bloc GAUCHE (Ma carrière + Profil reconversion + Mon Profil) -->
    <div class="dashboard-left">

      <!-- 🔹 Ma carrière -->
      <div class="dashboard-career">
        <h2 class="dashboard-section-title">Ma carrière</h2>
        <div class="dashboard-career-box">
          <p class="dashboard-career-title">Ébéniste</p>
          <a href="#" class="dashboard-career-btn">J’accède à ma carrière</a>
        </div>
      </div>

      <!-- 🔹 Mon profil de reconversion -->
      <div class="dashboard-profile">
        <h2 class="dashboard-section-title">Mon profil de reconversion</h2>
        <div class="dashboard-profile-box">
          <p class="dashboard-profile-title">Manuel(le) et Concret(e)</p>
          <%= link_to "Revoir les métiers", psy_profile_path(current_user.psy_profile.id), class: "dashboard-quiz-btn" %>
        </div>
      </div>

      <!-- 🔹 Mon Profil (Nouvelle section) -->
      <div class="dashboard-user">
        <h2 class="dashboard-section-title">Mon Profil</h2>
        <div class="dashboard-user-box">
          <% if current_user %>
            <p><strong>Nom :</strong> <%= current_user.first_name %> <%= current_user.last_name %></p>
            <p><strong>Email :</strong> <%= current_user.email %></p>
            <p><strong>Mot de passe :</strong> *******</p>
            <%= button_to "Déconnexion", destroy_user_session_path, method: :delete, class: "dashboard-logout-btn" %>
          <% else %>
            <p>Vous n'êtes pas connecté.</p>
            <div class="dashboard-profile-box">
              <%= link_to "Se connecter", new_user_session_path, class: "dashboard-login-btn" %>
            </div>
          <% end %>
        </div>
      </div>


    </div>

    <!-- 🔹 Bloc DROIT (Métiers enregistrés) -->
    <div class="dashboard-right">
      <h2 class="dashboard-section-title">Mes métiers enregistrés</h2>
      <div class="dashboard-jobs">
        <!-- Métier principal -->
        <div class="dashboard-job-large">
          <img src="<%= asset_path('ebeniste.png') %>" alt="Ébéniste" class="dashboard-job-image">
          <p>Ébéniste</p>
          <button class="dashboard-close-btn">✖</button>
        </div>
        <!-- Autres métiers -->
        <div class="dashboard-job-small">
          <img src="<%= asset_path('cuisinier.png') %>" alt="Cuisinier" class="dashboard-job-image">
          <p>Cuisinier</p>
          <button class="dashboard-close-btn">✖</button>
        </div>
        <div class="dashboard-job-small">
          <img src="<%= asset_path('mecanicien.png') %>" alt="Mécanicien" class="dashboard-job-image">
          <p>Mécanicien</p>
          <button class="dashboard-close-btn">✖</button>
        </div>
        <div class="dashboard-job-small">
          <img src="<%= asset_path('technicien.png') %>" alt="Technicien" class="dashboard-job-image">
          <p>Technicien</p>
          <button class="dashboard-close-btn">✖</button>
        </div>
      </div>
    </div>
  </div>
</div>
