<div class="dashboard-container">
  <!-- 🔹 En-tête -->
  <h1 class="dashboard-title">Tableau de bord</h1>

  <div class="dashboard-main">
    <!-- 🔹 Bloc GAUCHE -->
    <div class="dashboard-left">

      <!-- 🔹 Ma carrière -->
      <div class="dashboard-career">
        <div class="dashboard-career-header">
          <h2 class="dashboard-section-title">Ma carrière</h2>
        </div>
        <% if @career %>
          <%= link_to @career.job.title.capitalize, career_path(@career), class: "dashboard-btn dashboard-btn-primary" %>
          <%= link_to "J’accède à ma carrière", career_path(@career), class: "dashboard-btn dashboard-btn-secondary" %>
        <% else %>
          <%= link_to "Commencer une carrière", careers_path, data: { turbo_method: :post }, class: "career-btn career-primary" %>
        <% end %>
      </div>

      <!-- 🔹 Mon profil de reconversion -->
      <div class="dashboard-profile">
        <h2 class="dashboard-section-title">Mon profil de reconversion</h2>
        <p class="dashboard-profile-title"><%= @psy_profile.name %></p>
        <%= link_to "Revoir les métiers", psy_profile_path(@psy_profile), class: "dashboard-quiz-btn" %>
      </div>

      <!-- 🔹 Mon Profil -->
      <div class="dashboard-user">
        <h2 class="dashboard-section-title">Mon Profil</h2>
        <% if current_user %>
          <p><strong>Nom :</strong> <%= current_user.first_name %> <%= current_user.last_name %></p>
          <p><strong>Email :</strong> <%= current_user.email %></p>
          <p><strong>Mot de passe :</strong> *******</p>
          <%= button_to "Déconnexion", destroy_user_session_path, method: :delete, class: "dashboard-logout-btn" %>
        <% else %>
          <p>Vous n'êtes pas connecté.</p>
          <%= link_to "Se connecter", new_user_session_path, class: "dashboard-login-btn" %>
        <% end %>
      </div>
    </div>

    <!-- 🔹 Bloc DROIT -->
    <div class="dashboard-right">
      <h2 class="dashboard-section-title">Mes métiers enregistrés</h2>
      <div class="dashboard-jobs">
        <div class="dashboard-job-featured">
          <img src="<%= asset_path('ebeniste.png') %>" alt="Ébéniste" class="dashboard-job-featured-image">
          <p class="dashboard-job-title">Ébéniste</p>
        </div>
      </div>
    </div>
  </div>
</div>
